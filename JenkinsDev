pipeline {
    agent any

    stages {
        stage('Test Docker') {
            steps {
                sh '''
                    docker compose version
                '''
            }
        }
    }
}